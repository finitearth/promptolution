_target_: promptolution.optimizers.CAPO
crossovers_per_iter: 4
upper_shots: 5
length_penalty: 0.05
# CAPO uses block-based racing; pair with a block eval_strategy, e.g. task.eval_strategy=sequential_block
