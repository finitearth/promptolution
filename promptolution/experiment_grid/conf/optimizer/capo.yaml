_target_: promptolution.optimizers.CAPO
crossovers_per_iter: 4
upper_shots: 5
length_penalty: 0.05
# CAPO races prompts on data blocks — the task needs a block eval_strategy (the shipped tasks have one)
