[tool.poetry]
name = "promptolution"
version = "2.3.0"
description = "A framework for prompt optimization and a zoo of prompt optimization algorithms."
authors = ["Tom Zehle, Moritz Schlager, Timo Heiß, Henri Oberpaur"]
readme = "README.md"
include = [{ path = "promptolution/experiment/conf/**/*.yaml", format = ["sdist", "wheel"] }]

[tool.poetry.scripts]
promptolution-experiment = "promptolution.experiment.launch:main"

[tool.poetry.dependencies]
python = ">=3.10,<3.13"
numpy = ">=1.26.0, <3.0.0"
pandas = ">=2.2.2"
tqdm = ">=4.66.5"
scikit-learn = ">=1.5.2"
fastparquet = ">=2024.11.0"
openai = ">=1.0.0"
vllm = {version = ">=0.13.0", optional = true}
transformers = {version = ">=4.48.0", optional = true}
accelerate = {version = ">=0.20.0", optional = true}
hydra-core = ">=1.3.0"
hydra-submitit-launcher = {version = ">=1.2.0", optional = true}
datasets = {version = ">=2.0.0", optional = true}
scipy = ">=1.15"

[tool.poetry.extras]
vllm = ["vllm"]
transformers = ["transformers", "accelerate"]
experiment = ["hydra-submitit-launcher", "datasets"]

[tool.poetry.group.experiment]
optional = true
[tool.poetry.group.experiment.dependencies]
hydra-submitit-launcher = ">=1.2.0"
datasets = ">=2.0.0"

[tool.poetry.group.vllm]
optional = true
[tool.poetry.group.vllm.dependencies]
vllm = ">=0.13.0"

[tool.poetry.group.transformers]
optional = true
[tool.poetry.group.transformers.dependencies]
transformers = ">=4.48.0"
accelerate = ">=0.20.0"

[tool.poetry.group.dev.dependencies]
black = ">=24.4.2"
flake8 = ">=7.1.0"
isort = ">=5.13.2"
pre-commit = ">=3.7.1"
ipykernel = ">=6.29.5"
mypy = ">=1.8.0"

[tool.poetry.group.test.dependencies]
pytest = ">=8.3.5"
pytest-cov = ">=6.1.1"
transformers = ">=4.48.0"
vllm = ">=0.13.0"
torch = ">=2.0.0"

[tool.poetry.group.docs.dependencies]
mkdocs = ">=1.6.1"
mkdocs-material = ">=9.5.39"
mkdocstrings = {version = ">=0.26.1", extras = ["python"]}
jupyter = ">=1.1.1"
nbconvert = ">=7.16.6"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.black]
line-length = 120

[tool.isort]
line_length = 120
profile = "black"
sections = ["FUTURE", "STDLIB", "THIRDPARTY", "TYPING", "FIRSTPARTY", "PROMPTOLUTION", "LOCALFOLDER"]
known_typing = ["typing"]
known_promptolution = ["promptolution"]
lines_between_sections = 1

[tool.pydocstyle]
convention = "google"

[tool.mypy]
ignore_missing_imports = true
disable_error_code = ["call-overload", "assignment", "overload-overlap"]

[[tool.mypy.overrides]]
module = "transformers.*"
ignore_errors = true
