[task]
task_name = trec
ds_path = data_sets/cls
random_seed = 42
steps = 12

[optimizer]
name = evopromptga
selection_mode = random
meta_prompt_path = templates/evoprompt_ga_template.txt

[meta_llm]
name = local-gpt2
batch_size = 8

[downstream_llm]
name = local-gpt2
batch_size = 32

[logging]
dir = logs/gpt2-agnews-ga